
#include <stdio.h>
#include <stdlib.h>

int main (void) // for now, no parameters
{
   // 1. Set the config values (duration between questions, etc).
   // 2. Load the database.
   // 3. Choose a question.
   // 4. Present the question.
   //    a. If a new question is given, add it to the database
   //    b Update the database (presentation-counter++)
   //    c. If the answer is correct, update the database (correct-counter++)
   // 5. Save the database to file.
   // 6. Sleep for duration.
   // 7. Goto #2.

   return EXIT_SUCCESS;
}
